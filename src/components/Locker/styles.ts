import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        boxSizing: 'border-box',
        width: '100%',
        marginHorizontal: 20,
        flexDirection: 'row',
        padding: 30,
        borderRadius: 25,
    },
    image: {
        width: 35,
        height: 60,
    },
    textContainer: {
        height: '100%',
        marginLeft: 16,
    },
});