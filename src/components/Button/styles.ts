import { StyleSheet } from "react-native";


export const styles = StyleSheet.create({
    container: {
        width: '90%',
        paddingVertical: 15,
        marginBottom: 40,
        borderRadius: 15,
    },
    content: {
        height: 30,
        justifyContent: 'center',
        alignItems: 'center',
    },
})